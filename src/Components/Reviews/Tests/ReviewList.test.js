/* eslint-disable no-undef */
import Reviews from '../Reviews.jsx';
import ReviewList from '../ReviewList.jsx';
import ReviewListEntry from '../ReviewListEntry.jsx';
import ReviewBreakDown from '../ReviewBreakdown.jsx';
import RatingStars from '../RatingStars.jsx';
import AddReview from '../AddReview.jsx';


//Reviews.jsx

test('Reviews Should be a function', () => {expect(typeof Reviews).toEqual('function')})

//ReviewList.jsx
test('ReviewList Should not be Null', () => {expect(ReviewList).toBeDefined})

test('Review Should be a function', () => {expect(typeof Reviews).toEqual('function')})

//ReviewListEntry.jsx

test('ReviewListEntry Should be a function', () => {expect(typeof ReviewListEntry).toEqual('function')})

//ReviewBreakDown.jsx

test('ReviewBreakDown Should be a function', () => {expect(typeof ReviewBreakDown).toEqual('function')})

//AddReview.jsx

test('AddReview Should be a function', () => {expect(typeof AddReview).toEqual('function')})

//RatingStars.jsx

test('RatingStars Should be a function', () => {expect(typeof RatingStars).toEqual('function')})